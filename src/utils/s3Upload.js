// import Storage from '@aws-amplify/storage'

// export const S3Upload = (imageName, file, customPrefix) => Storage.put(imageName, file,
//   { contentType: file.type, customPrefix: customPrefix })

// export const S3Delete = (imageName, customPrefix) => Storage.remove(imageName,
//   { customPrefix: customPrefix })
